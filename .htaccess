# Enable RewriteEngine
RewriteEngine On

# If the request is not for an existing file, add .html (exclude api folder)
RewriteCond %{REQUEST_URI} !^/analisa/api/
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^([^\.]+)$ $1.html [NC,L]
